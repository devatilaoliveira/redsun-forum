spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

supabase:
  url: ${SUPABASE_URL}
  secret-key: ${SUPABASE_SECRET_KEY}
  storage-url: ${}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
